#!/bin/sh

if [ $# -gt 0 ]; then
	if [ $1 = GCC ]; then 
		mkdir build
		cd build
		cmake .. -G "Unix Makefiles" -DCMAKE_BUILD_TYPE=%configuration%
		make
		cd ..
	elif [ %1 = CLANG ]; then
		mkdir build
	fi
fi